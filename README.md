# Surveillant 
simple embedded dashboard written in C++ for linux embedded device, using LVGL graphical library

build instructions:
```
cd src
cmake -S ./ -B ./build
cd build && make
```

running target:
```
./surveillant
```
